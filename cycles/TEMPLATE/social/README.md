# Social

4 LinkedIn + 4 Instagram drafts. Format: `social/queue/README.md`.
Each has front matter incl. `image:` (required for Instagram) + `image_alt:` —
URLs come from `../images.md` (Unsplash, verified). Copied to `social/queue/<date>/`.
